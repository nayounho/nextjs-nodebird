export declare type IOrderHooksOptions = {
    groups: string[];
};
export declare type ISchemaOptions = IOrderHooksOptions;
export declare type IContext = {
    report(node: any, message: string): void;
    options: ISchemaOptions[];
};
export declare type INode = {
    kind: 'var' | 'let' | 'const';
    declarations: {
        init: {
            type: 'CallExpression';
            callee: {
                name: string;
            };
        };
    }[];
};
